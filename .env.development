# Local development (`npm run dev`)
# Copied into the client bundle — only VITE_* keys.

# Prefer secrets in gitignored `.env.local` (see `.env.example`).
# Audio for local practice files under ./audio/recitations
VITE_AUDIO_BASE_URL=/audio/recitations
