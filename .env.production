# Live / production (`npm run build`, GitHub Pages deploy)
# Copied into the client bundle — only VITE_* keys.

# Remote MP3 host (BigRock). Do not use /audio/recitations here.
VITE_AUDIO_BASE_URL=https://audio.alquranqiratacademy.com/recitations

# Supabase is injected by GitHub Actions Variables on deploy.
# For a local production build (`npm run build`), put keys in `.env.local`.
